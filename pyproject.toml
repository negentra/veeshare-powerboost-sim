[project]
name = "veeshare-powerboost-sim"
version = "0.3.5"
description = "VeeShare PowerBoost Innovation Audit simulation"
requires-python = ">=3.11"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
